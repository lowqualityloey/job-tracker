{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Cors": {
    "AllowedOrigins": [
      "http://127.0.0.1:5173",
      "http://127.0.0.1:4173",
      "http://localhost:5173"
    ]
  },
  "Web": {
    "_comment": "BEHAVIOR-071 / DECISION-m4-auth-007 (a'): the API serves the built front end from its own origin, so a browser harness is same-site with the session cookie. Relative to the content root, i.e. the repo's `dist/`. The Cors origins above stay, because `npm run dev` on :5173 is still a different origin and still needs them. Serve-gate is Development-only; a Production boot serves nothing whatever this says.",
    "SpaRoot": "../../../dist"
  }
}
